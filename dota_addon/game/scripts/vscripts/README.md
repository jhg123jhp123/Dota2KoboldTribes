# VScript Modules

This folder will contain server-side Lua modules for the Dota 2 custom game.

Phase 1 structure:

- `core`: cross-system infrastructure contracts.
- `world`: world generation and query contracts.
- `data`: authored or converted runtime data.

Lua files in this phase are placeholders that document intended interfaces. They should not be wired into a playable addon yet.
